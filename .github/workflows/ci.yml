name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    name: Validate HTML
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate HTML
        run: npx --yes html-validate@10 index.html
